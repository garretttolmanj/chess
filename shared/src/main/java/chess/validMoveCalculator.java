package chess;

import java.util.ArrayList;
import java.util.Collection;

public class validMoveCalculator {
    public validMoveCalculator() {}

    public static Collection<ChessMove> findValidMoves(ChessBoard board, ChessPosition position, Collection<ChessMove> moves, ChessGame.TeamColor teamColor) {
        Collection<ChessMove> validMoves = new ArrayList<ChessMove>();
        return validMoves;
    }
}
